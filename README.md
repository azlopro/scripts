# Scripts

- [`ubuntu-hardening/`](ubuntu-hardening/) contains the staged Ubuntu Server
  audit, network repair, SPA firewall, SSH/TOTP, integrity, and verification
  bundle for `n0`.
- `startup.sh` is the pre-existing role-specific firewall/SSH script and is
  independent of the new bundle.
